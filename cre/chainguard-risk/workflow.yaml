# CRE Workflow Settings File — ChainGuard Risk Detection
# Run: cre workflow simulate chainguard-risk --target staging-settings

staging-settings:
  user-workflow:
    workflow-owner-address: ""
    workflow-name: "chainguard-risk"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"

production-settings:
  user-workflow:
    workflow-owner-address: ""
    workflow-name: "chainguard-risk"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
